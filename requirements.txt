# ── YouTube to Shorts Converter — 100% FREE Requirements ───────────────────
# Install with:  pip install -r requirements.txt

# Video download
yt-dlp>=2024.1.1

# AI transcription — runs LOCALLY on your machine (no API key needed)
openai-whisper>=20231117

# FREE LLM API — LLaMA-3 via Groq (get free key at console.groq.com)
groq>=0.5.0

# Video editing
moviepy>=1.0.3
imageio-ffmpeg>=0.4.9

# Web server
flask>=3.0.0
flask-cors>=4.0.0

# Environment variables (.env file support)
python-dotenv>=1.0.0

# Misc
numpy>=1.24.0
tqdm>=4.66.0
